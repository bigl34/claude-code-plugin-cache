{
  "name": "@local/plugin-cache",
  "version": "1.0.0",
  "description": "Shared persistent cache library for Claude Code plugins",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "plugin-cache": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepublish": "npm run build"
  },
  "keywords": [
    "cache",
    "plugin",
    "claude-code"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "files": [
    "dist/**/*"
  ]
}
